float(monthly_savings) = float(input("enter your monthly income: "))
float(monthly_expenses) = float(input("enter your monthly income: "))
float(monthly_savings) = float(monthly income) - float(monthly expenses)
float(annual_interest_rate) = 0.05
Projected_Savings = Monthly Savings * 12 + (Monthly Savings * 12 * (1 + annual_interest_rate)))
print(projected_savings)
